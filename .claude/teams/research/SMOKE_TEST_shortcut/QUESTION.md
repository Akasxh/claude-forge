# SMOKE_TEST_shortcut — deliberate short-circuit exemplar

This is NOT a real research session. It is a deliberately-shortcutted evidence
dir used by the audit script smoke test. The audit MUST return exit 1 with
≥ 10 violations when run against this workspace. Do not delete; do not expand.

Generated by `orchestration-full-activation-v1` empiricist pass 2026-04-12.

Usage:
    python3 ~/.claude/scripts/audit_evidence.py SMOKE_TEST_shortcut --gate=synthesis
    # expected: EXIT_CODE=1, ≥ 20 violations, "RESULT: FAIL"
